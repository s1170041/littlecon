<%= form_with(model: message_detail, local: true) do |form| %>
  <% if message_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_detail.errors.count, "error") %> prohibited this message_detail from being saved:</h2>

      <ul>
      <% message_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
